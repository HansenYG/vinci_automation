# Comma-separated origins allowed to call the API (CORS)
CORS_ORIGINS=http://localhost:5173

# Supabase (Project Settings -> API)
# Backend may use the service_role key (keep it secret, server-side only).
SUPABASE_URL=
SUPABASE_KEY=
